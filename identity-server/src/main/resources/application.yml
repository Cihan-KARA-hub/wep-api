spring:
  security:
    oauth2:
        client:
          registration:
            google:
              client-id: 415393799733-3jtofkhoc7dd0ronabt9acf7cb4du0t3.apps.googleusercontent.com
              client-secret: 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

  mail:
    host: smtp.gmail.com
    port: 587
    username: cihankara632@gmail.com
    password: ovtz mkng waxy bhvf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/advertisement_db
    username: ilan_admin
    password: ilan_admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    open-in-view: false
    generate-ddl: false
  sql:
    init:
      platform: postgres
  application:
    name: identity-server
security:
  jwt:
    token:
      secret-key: ${SECRET_KEY}

server:
  port: 8087

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}
  instance:
    preferIpAddress: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE



